# Optional local overrides (never commit real secrets)
# TRUST_LATTICE_DB=./data/trust-lattice.db
# TRUST_LATTICE_POLICY=./policies/default.json
# Required for MCP mutating tools (min 16 chars). Fail-closed when unset.
# TRUST_LATTICE_ADMIN_TOKEN=replace-with-a-long-random-secret
